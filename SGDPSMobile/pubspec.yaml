name: sgdps_mobile
description: "SGDPS Field Collector Mobile Application for Society & Puja Collection Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  geolocator: ^13.0.2
  intl: ^0.19.0
  shared_preferences: ^2.3.5
  share_plus: ^10.1.4
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
